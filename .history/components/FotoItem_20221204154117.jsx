import { View, Text } from 'react-native'
import React from 'react'

const FotoItem = ({ id, timestamp, uri }) => {
    return (
        <View>
            <Text>FotoItem</Text>
        </View>
    )
}